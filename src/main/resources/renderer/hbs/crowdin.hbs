<script type="text/javascript" nonce="{{this}}">
    var req = new XMLHttpRequest();
    req.open('GET', document.location, false);
    req.send(null);
    var languageCode = req.getResponseHeader('Content-Language').toLowerCase();
    if (languageCode == 'zy') {
        var _jipt = [];
        _jipt.push(['project', 'morpheus']);
        _jipt.push(['escape', function() {
          window.location.href = "/user-settings?lang=";
        }]);
        $.getScript( "//cdn.crowdin.com/jipt/jipt.js", function( data, textStatus, jqxhr ) {
        });
    }
</script>